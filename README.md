
**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`

2. Create .env.local file
    a. Linux:
    touch .env.local
    b. PowerShell
    New-Item .env.local -ItemType File
2. Set the `GEMINI_API_KEY` to your own GEMINI API
3. Run the app:
   `npm run dev`
